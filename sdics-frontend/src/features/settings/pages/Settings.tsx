import { Box, Typography } from '@mui/material'

export default function Settings() {
  return (
    <Box>
      <Typography variant="h4">Settings</Typography>
      <Typography sx={{ mt: 2, color: 'text.secondary' }}>Settings interface coming soon...</Typography>
    </Box>
  )
}
